name: "CodeQL Advanced"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '30 6 * * 0'

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ${{ (matrix.language == 'swift' && 'macos-latest') || 'ubuntu-latest' }}
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        include:
        - language: python
          build-mode: none

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v3
      with:
        languages: ${{ matrix.language }}
        build-mode: ${{ matrix.build-mode }}

    - if: matrix.build-mode == 'manual'
      shell: bash
      run: |
        echo 'If you are using a "manual" build mode for one or more of the' \
          'languages you are analyzing, replace this with the commands to build' \
          'your code, for example:'
        echo '  make bootstrap'
        echo '  make release'
        exit 1

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v3
      with:
        category: "/language:${{matrix.language}}"

  duplication-check:
    name: Detect Code Duplication
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Tüm commit geçmişini çek

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install jscpd
        run: npm install -g jscpd

      - name: Get changed files
        id: changed-files
        uses: tj-actions/changed-files@v44
        with:
          since: ${{ github.event.pull_request.base.sha }}
          until: ${{ github.event.pull_request.head.sha }}

      - name: Run jscpd on changed files
        run: |
          if [ -n "${{ steps.changed-files.outputs.all_changed_files }}" ]; then
      echo "Changed files: ${{ steps.changed-files.outputs.all_changed_files }}"
      jscpd \
        --min-tokens 10 \  # Daha küçük kod bloklarını tespit et
        --threshold 1 \    # %1 duplication threshold
        --reporters console \
        --ignore "**/generated/**" \  # İgnore edilecek dosyalar
        --ignore-identifiers \  # Değişken isimlerini ignore et
        ${{ steps.changed-files.outputs.all_changed_files }}
    else
      echo "No files changed. Skipping duplication check."
    fi
Sonuç
Bu kod, jscpd ile tarandığında yüksek bir duplication oranı gösterecektir. Bu sayede, workflow'unuzdaki duplication kontrolünü başarıyla test edebilirsiniz. Eğer başka sorularınız varsa veya daha fazla yardıma ihtiyacınız olursa, bana bildirin! 😊

New chat
